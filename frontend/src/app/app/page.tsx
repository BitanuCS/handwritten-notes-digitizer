// The tool itself: upload notes -> choose theme -> convert -> preview/download.
// Built out across Phases 2–8. Placeholder for now.

export default function AppPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8">
      <h1 className="text-2xl font-semibold">Digitize your notes</h1>
      <p className="mt-2 text-sm text-gray-500">
        Upload UI coming in the next phase.
      </p>
    </main>
  );
}
